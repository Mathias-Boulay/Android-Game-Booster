<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <item android:id="@android:id/background">
        <shape
            android:innerRadiusRatio="2.3"
            android:shape="ring"
            android:useLevel="false"
            android:type="sweep"
            android:thicknessRatio="15.0">
            <solid android:color="@color/pureWhite"/>
        </shape>
    </item>

    <item android:id="@android:id/progress">
        <rotate
            android:pivotX="50%"
            android:pivotY="50%"
            android:fromDegrees="-90"
            android:toDegrees="-90">
            <shape
                android:innerRadiusRatio="2.3"
                android:shape="ring"
                android:angle="0"
                android:thicknessRatio="15.0"
                >
                <gradient
                    android:angle="315"
                    android:centerX="0.5"
                    android:centerY="0.5"
                    android:centerColor="@color/backgroundDownStart"
                    android:endColor="@color/backgroundDownEnd"
                    android:gradientRadius="3000"
                    android:startColor="@color/backgroundDownEnd"
                    android:type="sweep"
                    android:useLevel="false"/>

            </shape>
        </rotate>
    </item>

</layer-list>